import React,{useEffect,useMemo,useState} from 'react'
import {AlertTriangle,Building2,CheckCircle2,Download,Globe2,MapPin,Radar,RefreshCw,Search,ShieldCheck,SquareStack,Truck,XCircle} from 'lucide-react'
import {exportCsv,investigateDealer,loadDealerState,runDealerJob} from './backendClient.mjs'
import ScanHUD from './ScanHUD.jsx'
import ContactsPanel from './ContactsPanel.jsx'
import VehicleInventoryPanel from './VehicleInventoryPanel.jsx'
import DealerPulsePanel from './DealerPulsePanel.jsx'

function Badge({children,tone='idle'}){return <span className={`di-badge di-${tone}`}>{children}</span>}
function statusTone(s){return s==='COMPLETE'?'good':s==='INCOMPLETE'?'warn':s==='ERROR'?'bad':'idle'}
const coverageStatus=d=>d.current_coverage?.status||d.latest_run?.status||'UNSCANNED'

export default function ScrapeItApp(){
  const [state,setState]=useState({dealers:[],stats:{dealer_count:0,scanned_count:0,incomplete_count:0,error_count:0,vin_count:0,material_changes:0,regions:{}}})
  const [selected,setSelected]=useState(new Set()),[query,setQuery]=useState(''),[region,setRegion]=useState('ALL'),[activeJobId,setActiveJobId]=useState(null),[running,setRunning]=useState(false),[message,setMessage]=useState(null),[probeUrl,setProbeUrl]=useState(''),[probeBusy,setProbeBusy]=useState(false),[probeResult,setProbeResult]=useState(null),[probeMessage,setProbeMessage]=useState(null)
  async function refresh(){const next=await loadDealerState();setState(next);return next}
  useEffect(()=>{refresh().catch(e=>setMessage({type:'error',text:e.message}))},[])
  const regions=useMemo(()=>['ALL',...Object.keys(state.stats?.regions||{}).sort()],[state.stats]),visible=useMemo(()=>state.dealers.filter(d=>{const platform=d.current_coverage?.platform||d.latest_attempt?.platform||d.latest_run?.platform||'';const hay=`${d.dealer_name} ${d.dealer_id} ${d.city} ${d.state} ${platform}`.toLowerCase();return(!query||hay.includes(query.toLowerCase()))&&(region==='ALL'||d.region===region)}),[state.dealers,query,region]),selectedIds=useMemo(()=>[...selected],[selected])
  function toggle(id){setSelected(cur=>{const n=new Set(cur);n.has(id)?n.delete(id):n.add(id);return n})}
  function choose(kind){let ids=[];if(kind==='ALL')ids=visible.map(d=>d.dealer_id);if(kind==='FAILED')ids=visible.filter(d=>coverageStatus(d)!=='COMPLETE'&&(d.latest_attempt||d.latest_run)?.status==='ERROR').map(d=>d.dealer_id);if(kind==='INCOMPLETE')ids=visible.filter(d=>coverageStatus(d)!=='COMPLETE').map(d=>d.dealer_id);setSelected(new Set(ids))}
  async function runInventory(){if(!selected.size)return setMessage({type:'warning',text:'Select at least one dealer.'});setRunning(true);setMessage(null);try{let jobId=null;await runDealerJob('INVENTORY',selectedIds,{concurrency:4,onProgress:x=>{jobId=x.jobId;setActiveJobId(x.jobId)}});if(jobId)setActiveJobId(jobId);const next=await refresh();const complete=selectedIds.filter(id=>coverageStatus(next.dealers.find(d=>d.dealer_id===id)||{})==='COMPLETE').length;setMessage({type:'success',text:`Inventory run finished for ${selectedIds.length} dealer(s). ${complete} have current completeness-proven coverage.`})}catch(e){setMessage({type:'error',text:e.message})}finally{setRunning(false)}}
  async function probeDealer(){if(!probeUrl.trim())return setProbeMessage({type:'warning',text:'Enter a dealer website first.'});setProbeBusy(true);setProbeMessage(null);setProbeResult(null);try{const result=await investigateDealer(probeUrl.trim());setProbeResult(result);setProbeMessage({type:result.coverage_status==='COMPLETE'?'success':'warning',text:result.coverage_status==='COMPLETE'?'One-time scan proved exhaustive public inventory coverage.':'One-time scan captured evidence, but exhaustive coverage was not proven.'})}catch(e){setProbeMessage({type:'error',text:e.message})}finally{setProbeBusy(false)}}
  function exportDealers(){exportCsv('scrape-it-dealer-status.csv',visible.map(d=>{const cov=d.current_coverage||{},attempt=d.latest_attempt||d.latest_run||{};return{dealer_id:d.dealer_id,dealer_name:d.dealer_name,city:d.city,state:d.state,region:d.region,current_coverage:coverageStatus(d),latest_attempt_status:attempt.status||'UNSCANNED',platform:cov.platform||attempt.platform||'',adapter:cov.adapter_name||'',vin_count:d.latest_snapshot?.vin_count||0,coverage_observed_at:cov.observed_at||'',latest_attempt_at:attempt.finished_at||attempt.started_at||'',inventory_url:cov.inventory_url||attempt.inventory_url||'',completeness_reason:cov.completeness_reason||'',latest_attempt_error:attempt.error_message||''}}))}
  return <div className="scrape-it-shell"><header className="si-header"><div><div className="si-brand"><Radar size={25}/><span>Scrape It</span></div><p>Fast public-web dealer inventory + decision-maker discovery</p></div><div className="si-header-badges"><Badge tone="good">SCRAPE-IT PREVIEW</Badge><Badge tone="idle">MAIN UNTOUCHED</Badge></div></header><main className="si-main">
    <section className="si-hero si-hero-compact"><div><p className="si-kicker">Operator console</p><h1>Pick dealers. Scrape them. Keep the evidence.</h1><p>Inventory and public staff discovery stay separate, source-backed, and reviewable. COMPLETE means exhaustive public inventory coverage was proven; anything less stays observation-only.</p></div><div className="si-hero-metric"><span>Current book</span><strong>{state.stats.dealer_count||state.dealers.length} dealers</strong><p>{state.stats.scanned_count||0} complete · {state.stats.incomplete_count||0} incomplete · {state.stats.error_count||0} errors without usable coverage</p></div></section>
    <DealerPulsePanel/>
    <section className="si-panel"><div className="si-section-head"><div><p className="si-kicker">Dealer controls</p><h2>Select one, several, filtered groups, or the full book</h2></div><div className="si-header-badges"><Badge tone="good"><ShieldCheck size={13}/>EVIDENCE GATED</Badge><Badge tone="idle">{selected.size} selected</Badge></div></div><div className="si-grid si-grid-3"><article className="si-metric"><Building2/><span>Dealers</span><strong>{state.stats.dealer_count||state.dealers.length}</strong><small>Rickey book universe</small></article><article className="si-metric"><CheckCircle2/><span>Complete</span><strong>{state.stats.scanned_count||0}</strong><small>Current exhaustive public coverage</small></article><article className="si-metric"><Truck/><span>Verified VINs</span><strong>{state.stats.vin_count||0}</strong><small>From latest safe COMPLETE snapshots</small></article></div><div className="si-control-bar"><label><Search size={15}/><input placeholder="Dealer, ID, city, state, platform" value={query} onChange={e=>setQuery(e.target.value)}/></label><select value={region} onChange={e=>setRegion(e.target.value)}>{regions.map(r=><option key={r}>{r}</option>)}</select><button onClick={()=>choose('ALL')}>Select visible</button><button onClick={()=>choose('INCOMPLETE')}>Needs coverage</button><button onClick={()=>choose('FAILED')}>Hard errors</button><button onClick={()=>choose('CLEAR')}>Clear</button></div><div className="si-run-strip"><button className="si-primary" disabled={running||!selected.size} onClick={runInventory}><RefreshCw size={16} className={running?'si-spin':''}/>{running?'Scraping selected…':`Scrape inventory (${selected.size})`}</button><button className="si-secondary" onClick={exportDealers}><Download size={15}/>Export dealer status</button><span>{visible.length} visible</span></div>{message&&<div className={`di-message di-message-${message.type}`}>{message.text}</div>}<div className="si-dealer-grid">{visible.map(d=>{const attempt=d.latest_attempt||d.latest_run,cov=d.current_coverage||{},snap=d.latest_snapshot,status=coverageStatus(d),attemptIssue=attempt&&attempt.status!=='COMPLETE'&&status==='COMPLETE';return <button key={d.dealer_id} className={`si-dealer-card ${selected.has(d.dealer_id)?'selected':''}`} onClick={()=>toggle(d.dealer_id)}><div className="si-dealer-card-head"><span className="si-check">{selected.has(d.dealer_id)?'✓':''}</span><div><strong>{d.dealer_name}</strong><small>DLR #{d.dealer_id}</small></div><Badge tone={statusTone(status)}>{status}</Badge></div><div className="si-dealer-card-body"><span><MapPin size={13}/>{d.city}, {d.state} · {d.region}</span><span><Globe2 size={13}/>{cov.platform||attempt?.platform||'Platform pending'}</span><span><SquareStack size={13}/>{snap?.vin_count??0} current proven VINs</span></div>{attemptIssue&&<small className="si-card-warning"><AlertTriangle size={12}/>Current coverage remains valid; latest attempt was {attempt.status}.</small>}{status!=='COMPLETE'&&attempt?.status==='ERROR'&&<small className="si-card-error"><XCircle size={12}/>{attempt.error_message}</small>}{status!=='COMPLETE'&&attempt?.status==='INCOMPLETE'&&<small className="si-card-warning"><AlertTriangle size={12}/>Observation captured; exhaustive coverage not proven.</small>}</button>})}</div></section>
    <section className="si-panel"><div className="si-section-head"><div><p className="si-kicker">One-time investigation</p><h2>Scan a dealer outside the current book</h2><p className="di-sub">Paste a public dealer website. This probe does not add the rooftop to monitored dealers and does not alter Rickey's book.</p></div><Badge tone="idle">OBSERVATION ONLY</Badge></div><div className="si-control-bar"><label><Globe2 size={15}/><input placeholder="https://www.exampledealer.com" value={probeUrl} onChange={e=>setProbeUrl(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')probeDealer()}}/></label><button className="si-primary" onClick={probeDealer} disabled={probeBusy}>{probeBusy?'Investigating…':'Investigate dealer'}</button></div>{probeMessage&&<div className={`di-message di-message-${probeMessage.type}`}>{probeMessage.text}</div>}{probeResult&&<div className="si-grid si-grid-3"><article className="si-metric"><Globe2/><span>Platform</span><strong>{probeResult.platform||'Unknown'}</strong><small>{probeResult.website}</small></article><article className="si-metric"><Truck/><span>Validated VINs</span><strong>{probeResult.vin_count||0}</strong><small>{probeResult.pages_scanned||0} page(s) scanned</small></article><article className="si-metric"><ShieldCheck/><span>Coverage</span><strong>{probeResult.coverage_status||'INCOMPLETE'}</strong><small>{probeResult.completeness_reason}</small></article></div>}<div className="di-foot"><strong>Truth rule:</strong> One-time investigation remains public-web observation unless exhaustive coverage is proven. It does not create a monitored dealer record.</div></section>
    <VehicleInventoryPanel dealerState={state} selectedIds={selectedIds}/><ContactsPanel dealers={state.dealers} selectedIds={selectedIds} onJob={setActiveJobId}/><section className="si-guardrail"><ShieldCheck/><div><strong>Truth boundary</strong><p>Inventory disappearance is recorded as “no longer observed,” never automatically as a sale or trade. Public contact pages are evidence of what was represented when observed; private CRM records change only after an explicit Match / Update / Add decision.</p></div></section>
  </main><ScanHUD jobId={activeJobId} onState={s=>{const j=s.jobs?.[0];if(j?.status==='COMPLETE')refresh().catch(()=>{})}}/></div>
}
