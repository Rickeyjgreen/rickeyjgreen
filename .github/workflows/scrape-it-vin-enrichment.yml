name: scrape it vin enrichment (retired donor job)

# No schedule or writer remains on the canonical branch. The donor Supabase
# project is an archive; BYBO research and the BYBO-only pilot own new scans.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Retired donor job. Use the BYBO research workspace."
